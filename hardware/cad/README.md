# OSSM CAD — release STEP exports

Export from Onshape for the tagged release:

- One `.step` per required component
- `ossm-asm.step` — full assembly for the release tag

Do not commit Fusion working trees or STLs.
